import tkinter

button_value = [
    ["ac", "-/+", "+", "-"],√
    ["1", "2", "3", "4"]
]
# top_value = len(button_value)
# right = len(button_value[0])

color = "#ffffffdc"
color2 = "#82d91f"

window = tkinter.Tk()
window.title()
window.resizable(False, False)